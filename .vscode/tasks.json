{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build",
            "type": "shell",
            "command": "make",  // Just call make instead!
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}